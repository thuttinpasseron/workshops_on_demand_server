# HPEDEV Hack Shack Notebooks On Demand Server
### Getting Started
- Create a `.env` from the `.env.example`
- Install dependencies by running `yarn` or `npm i` in the project folder
- Start the API `npm run start` or `yarn start`